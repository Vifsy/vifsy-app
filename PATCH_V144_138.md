# Spreelo v144.138 — Transparent product composition correction

Built from `spreelo-144.137-VERCEL-BUILD-FIX-FULL.zip`.

## Fixed

### Transparent Product post images
- Removed the transparent-path wording that allowed lighting falloff / environmental effects around the future product area.
- Transparent Product posts now use a dedicated background + typography prompt.
- GPT-image-2 is instructed to generate only the background/environment and typography layer.
- The original transparent product is still composited afterward as the single source of truth.
- The reserved product area is kept visually clear and free of text/foreground objects before compositing.
- No product reference image is sent into the image-generation call in this transparent path.

### Unchanged
- Non-transparent Product posts still use the existing high-fidelity GPT-image-2 recreation path.
- Product ad with text is unchanged.
- Product carousel is unchanged.
- Product Reel / animated product is unchanged.
- AI product video is unchanged.
- Other content types are unchanged.
- Calendar-campaign Product posts still use the same Product-post generator and retain campaign context.
- Exact visible-copy locking from v144.136/137 remains in place.

## Verification
- `node --check app/api/cron/run-automations/route.js` passed.
- `node --check app/api/admin/post-approvals/regenerate-product/route.js` passed.
- Static transparent-prompt check confirms the removed lighting/shadow language is absent from the transparent branch.
- Diff against v144.137 shows only `app/api/cron/run-automations/route.js` plus this patch note changed.
- No SQL/database migration required.
