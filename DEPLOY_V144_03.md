# Deploy v144.03

No SQL migration is required.

Deploy the full package as usual. Existing TikTok environment variables from v144.02 remain valid.

After deploy, regenerate an existing TikTok post (or create a new one) so it receives the clean TikTok media override. Then test in this order: single image, carousel, animated video.
