# Spreelo v144.77 — sidebar underlay polish

## Change
- The desktop Settings shell now paints the full 255 px sidebar column with the same navy sidebar gradient behind the actual sidebar.
- This prevents the warm/beige workspace background from appearing above or behind the sidebar in long-page scrolled screenshots.
- The visible sidebar itself is unchanged.
- Mobile/tablet behavior below 801 px is unchanged.

## Functional safety
- No billing UI logic changed.
- No plan entitlement values changed.
- No Stripe handlers changed.
- No credit logic changed.
- No database/schema changes.
- No SQL required.
