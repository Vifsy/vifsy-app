# Spreelo v144.100 — Campaign Overview Image Polish

- Built on top of v144.99.
- Fixed the calendar-campaign overview image so it no longer stretches into an oversized square/portrait block at intermediate desktop/tablet widths.
- Removed the extra visual frame/pseudo-layer around the campaign image that could appear as a line outside the rounded image.
- Added a restrained dark gradient overlay at the bottom of the campaign image with the active campaign name, so the visual is clearly tied to the campaign.
- Kept the campaign visual in a controlled responsive aspect ratio: compact on desktop/tablet and full-width without excessive height on mobile.
- Removed the redundant `Frekvens` and `Du kan ändra` fact cards from the top metadata grid. The overview now keeps only `Kampanjperiod` and `Totalt planerade inlägg` there.
- The more useful `Kan ändras av dig` guidance card remains lower in the overview because it explains the customer's editing freedom in context.
- No SQL changes.
