# v144.93 — Home mobile reference polish

- Mobile-only refinement of the approval card and `Innehållsplaner` on Home.
- Approval card is more compact and the primary CTA includes the reference arrow.
- `Innehållsplaner` uses one grouped three-row panel with a mobile-only `Visa alla` action.
- Existing row actions and schedule managers remain functional.
- Desktop/tablet and all other Home sections are unchanged.
- No SQL changes.
