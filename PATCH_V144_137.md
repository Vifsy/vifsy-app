# Spreelo v144.137 — Vercel build fix

Built from v144.136 worktree.

## Fixed
- Corrected malformed JavaScript introduced in the v144.136 visible-copy helper.
- `.split("\n")` is now emitted as a valid JavaScript string literal instead of a literal newline inside quotes.

## Preserved from v144.136
- Transparent Product posts: GPT-image-2 generates background + typography without the product in the generation canvas, then Spreelo composites the original transparent product once.
- Non-transparent Product posts: GPT-image-2 keeps the high-fidelity recreation path.
- Product-post visible copy is supplied as exact preselected text where available to reduce spelling drift.
- Calendar-campaign Product posts use the same Product-post generator.

## Verification
- `node --check app/api/cron/run-automations/route.js` passed.
- `node scripts/test-v144-137-editorial-product-posts.mjs` passed.
- ZIP root structure verified; no wrapper directory.
