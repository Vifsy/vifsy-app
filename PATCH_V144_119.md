# Spreelo v144.119 — Brand Profile build fix

Fixes the Vercel/Turbopack CSS parsing error from v144.118.

- Corrected the malformed unquoted @import in app/globals.css.
- Added a regression guard for the stylesheet import.
- No SQL changes required.
