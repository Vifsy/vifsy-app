# Spreelo v144.117 — Brand Profile reference rebuild

## What changed
- Rebuilt the **read-only Brand Profile** view to match the supplied reference layout.
- Uses the same light magic background / translucent glass visual system as AI Content Studio.
- Hero now uses the current brand name dynamically, the existing Spreelo brand-intelligence artwork, and the edit action in the reference position.
- Replaced the old read-only arrow rows with:
  - Brand overview strip
  - Dynamic industry / market / language tags
  - Dynamic creation year, market and audience summary
  - 2 × 3 company-information grid
  - Existing functional logo controls and dynamic social-placement preview
- The social-preview brand name and logo/initials remain dynamic — no Pressit-specific text is hardcoded.
- Added dedicated tablet and mobile layouts down to narrow phone widths.
- Calibrated the danger zone to the compact reference rhythm.

## Safety / scope
- No database schema changes.
- No SQL needs to be run.
- No analysis, publishing, scheduling, logo-upload, save or delete logic was changed.
- The change is scoped to `/brand` with a final CSS layer so other Spreelo pages are not restyled.

## Verification
Run:

```bash
npm run test:v144.117
```
