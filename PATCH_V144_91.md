# Spreelo v144.91 — planned-post calendar polish

Targeted UI correction only for the date calendar opened from an expanded planned post when publishing once.

- Removed the extra white wrapper/frame around the selected-date control and calendar.
- Calendar now uses a normal readable 7-column layout instead of inheriting the narrow date field width.
- On phones, only while the calendar is open, the date editor receives the full available card width so weekdays and dates do not collapse.
- Selected-date button remains a separate clean control.
- No changes to Planinställningar, week rhythm, billing, plan limits, credit logic, publishing logic, or database schema.
- No SQL required.
