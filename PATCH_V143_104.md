# v143.104 — YouTube audience + Spreelo approval preview polish

- YouTube uploads now send `status.selfDeclaredMadeForKids` so the audience decision is set before upload.
- Added a per-YouTube-connection audience selector in Social channels.
- Added `social_connections.youtube_made_for_kids` via `supabase/v143_104_youtube_audience.sql`.
- Approval preview now uses the real Spreelo logo and a light Spreelo visual system.
- Removed the misleading muted-audio hint.
- Preview videos now loop continuously.
- Removed the customer/user initial avatar and personal-name row.
- Removed Request changes from the approval flow.
- Reject is now a small text link below the primary approval button.
- Desktop layout is viewport-height aware to avoid page scrolling at normal desktop sizes.
- Mobile layout is centered and scales the portrait video to the available width.
- Preserves v143.103 Shotstack content-sized HTML5 fix.
