# Spreelo v143.30.1

- Backfills the admin product editor from existing `post_slides` for posts created before v143.30.
- Shows a clear Remove control on every editable product row.
- Fixes admin regeneration by including the required `user_id` and complete slide fields.
- Keeps product URL optional.
- Restores the previous carousel slides if the replacement insert fails.
- Updates post slide counts and render status after successful regeneration.

No additional database migration is required beyond `v143_30_review_workbench_calendar_assets.sql`.
