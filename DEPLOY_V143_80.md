# Deploy Spreelo v143.80

1. Deploy the complete v143.80 project to Vercel.
2. No SQL migration is required.
3. Existing Stripe environment variables/webhook configuration remain unchanged.
4. Verify `/settings` on desktop and `/automation` on mobile after deployment.

Expected Settings desktop structure: hero + quick cards in one top row, then full-width billing/pricing below.
Expected AI Studio mobile behavior: narrow outer gutter, no overlapping platform explanation, helper text stays inside setting cards.
