# Spreelo v144.94 — Home desktop Content plans alignment

- Wide desktop only (`min-width: 1451px`).
- Keeps the v144.93 mobile Home reference unchanged.
- Gives the status and action areas in `Innehållsplaner` dedicated right-aligned columns.
- Empty statuses (`Inga aktiva`, `Inget planerat`) stay on one line with smaller, calmer typography.
- Actions (`Skapa schema`, `Planera inlägg`, `Skapa kampanj`) stay on one line with their arrow directly beside the label.
- No logic, product engine, billing, credits, AI Studio, calendar, or database changes.
- No SQL required.
