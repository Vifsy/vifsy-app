# Deploy v143.92

Ingen ny SQL-migration krävs.

Profilbilder använder den befintliga publika bucketen `user-avatars` och metadatafälten `spreelo_avatar_url` och `spreelo_avatar_path`.

Varumärkeslogotyper använder den befintliga brand-assets-konfigurationen och fälten `logo_url`, `logo_storage_path` och `logo_enabled_by_default`.

