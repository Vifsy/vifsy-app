// Deprecated compatibility shim.
// English is the only source UI language. All non-English UI labels are loaded
// from persistent ui_translation_packs and generated only for missing/changed keys.
export function getBuiltInLocaleLabel() {
  return "";
}
